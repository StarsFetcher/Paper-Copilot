# ============================================================
# Paper-Copilot 项目依赖
# ============================================================
# 学术论文检索与整理 Agent 系统
#
# 安装方式:
#   pip install -r requirements.txt
#
# 建议使用虚拟环境:
#   python -m venv venv
#   venv\Scripts\activate     (Windows)
#   source venv/bin/activate  (Linux/Mac)
#   pip install -r requirements.txt
# ============================================================

# ----------------------------------------------------------
# Web 框架
# ----------------------------------------------------------
flask>=3.0,<4.0

# ----------------------------------------------------------
# LangChain 核心（版本 0.1.0+ 新式 API）
# 注: langchain 1.x 中，AgentExecutor 等经典组件位于 langchain-classic 包
# ----------------------------------------------------------
langchain>=0.3.0
langchain-classic>=1.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# ----------------------------------------------------------
# LangGraph — 有状态 Agent 编排（替代 AgentExecutor）
# ----------------------------------------------------------
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=1.0.0   # 可选：持久化检查点（SqliteSaver）

# ----------------------------------------------------------
# 论文发现 (arXiv API)
# ----------------------------------------------------------
arxiv>=2.0.0

# ----------------------------------------------------------
# Embedding 模型
# ----------------------------------------------------------
# OpenAI 兼容的远程 Embedding（langchain-openai 已在上面引入）
# 本地 HuggingFace Embedding（推荐 DeepSeek 用户）
sentence-transformers>=3.0.0
langchain-huggingface>=0.1.0

# ----------------------------------------------------------
# PDF 解析引擎
# ----------------------------------------------------------
PyMuPDF>=1.23.0
pdfplumber>=0.10.0

# ----------------------------------------------------------
# 配置与环境变量管理
# ----------------------------------------------------------
python-dotenv>=1.0.0

# ----------------------------------------------------------
# 数据校验
# ----------------------------------------------------------
pydantic>=2.0.0

# ----------------------------------------------------------
# HTTP 客户端（OpenAI SDK 底层依赖）
# ----------------------------------------------------------
httpx>=0.25.0

# ----------------------------------------------------------
# 其他工具
# ----------------------------------------------------------
tiktoken>=0.5.0      # Token 计数（OpenAI 系列模型）
